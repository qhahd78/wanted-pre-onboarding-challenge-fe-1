import React from "react";
import Pannel from "./Pannel";
import { HomePannelContainer } from "./style";

const HomePannel = () => {
  return (
    <HomePannelContainer>
      <Pannel />
    </HomePannelContainer>
  );
};

export default HomePannel;
